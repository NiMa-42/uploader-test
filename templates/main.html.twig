{% extends './base.html.twig' %}

{% block body %}
    <section class="container mx-auto my-24">
        <div class="flex mb-8">
            <img src="{{ asset('images/logo.svg') }}" alt="Logo even bijzetten">
        </div>
        <div class="bg-white p-8 rounded-2xl shadow-lg w-full max-w-md text-center">
            <h1 class="text-2xl font-bold text-primary mb-4 font-noto">Bestandconversie demo</h1>
            <p class="text-gray-600 mb-6">Upload een CSV of XLSX bestand om dit om te zetten.</p>

            <div id="uploader"></div>
        </div>
    </section>

{% endblock %}
